#templateid:NetCode.RemoteStructSerializerRegistrationSystem.cs
// THIS FILE IS AUTO-GENERATED BY NETCODE PACKAGE SOURCE GENERATORS. DO NOT DELETE, MOVE, COPY, MODIFY, OR COMMIT THIS FILE.
// TO MAKE CHANGES TO THE SERIALIZATION OF A TYPE, REFER TO THE MANUAL.
using System.Text;
using Unity.Entities;
using Unity.Burst;
using Unity.Collections;
using Unity.NetCode;
using Unity.NetCode.LowLevel.Unsafe;
#region __REMOTES_USING_STATEMENT__
using __REMOTES_USING__;
#endregion

namespace __REMOTES_NAMESPACE__
{
    //[BurstCompile]
    [System.Runtime.CompilerServices.CompilerGenerated]

    [CreateBefore(typeof(DefaultVariantSystemGroup))]
    [CreateAfter(typeof(RemotesAutoInvokeSystem))]
    [UpdateInGroup(typeof(Internal_RemoteAutoInvokeSystemGroup))]
    public partial struct RemoteAutoInvokeRegistrationSystem : ISystem
    {
        /// <summary>TODO - Not currently burst compiled because we are actually passing static delegate (managed). In order to get
        /// this burst-compile properly, we need to use either ManagedFunctionPointer wrapper (we have all the machinery for this already)</summary>
        /// <param name="state"></param>
        public void OnCreate(ref SystemState state)
        {
            #region __REMTOES_AUTO_INVOKE_LIST__
            Netcode.Remote.RegisterAutoInvokeType(__REMOTES_NAME__.HandleFunctionID, __REMOTES_NAME__.StaticHandle, state.World); // OK so here we match the ID manually, we can probably do that with source gen
            #endregion
            state.Enabled = false;
        }
    }
}
